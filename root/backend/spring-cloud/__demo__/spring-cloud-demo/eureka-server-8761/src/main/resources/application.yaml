server:
  port: 8761

spring:
  application:
    name: EUREKA-SERVER

eureka:
  instance:
    hostname: localhost
  client:
    # 注册中心不需要检索服务
    fetch-registry: false
    # 注册中心不需要提供服务
    register-with-eureka: false
